# titulo

```txt
src/
├── assets/         # Images, fonts
├── components/     # Nested: /ui (shared), /features (domain-specific)
├── content/        # Content Collections (blog, products, docs)
├── layouts/        # Base layouts
├── pages/          # File-based routing (keep lightweight)
├── styles/         # Global styles/Sass/Tailwind
└── utils/          # TypeScript functions, helpers
```
